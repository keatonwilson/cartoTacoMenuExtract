# Spec Images Storage Bucket

Create a `spec-images` storage bucket in the Supabase dashboard for item_spec images.

1. Go to Supabase Dashboard > Storage
2. Click "New bucket"
3. Name: `spec-images`
4. Set to **public** (images are displayed in the app UI)
5. Click "Create bucket"
